export function Loading() {
  return (
    <div
      style={{
        backgroundColor: "rgba(0,0,0,0.05)",
        width: "100%",
        height: "100%",
        minHeight: 200,
      }}
    ></div>
  );
}
