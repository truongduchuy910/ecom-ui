export function NhaYenBg({ children }) {
  return (
    <div
      style={{
        background: "url(/assets/img/nha-yen-1.JPG)",
        backgroundSize: "cover",
      }}
    >
      {children}
    </div>
  );
}
